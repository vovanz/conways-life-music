/** Background of the entire canvas. */
export const COLOR_BACKGROUND   = '#000';

/** Fill color for dead cells across the whole infinite grid. */
export const COLOR_CELL_DEAD    = '#1c1c1c';

/** Fill color for live cells. */
export const COLOR_CELL_ALIVE   = '#f0f0f0';

/** Stroke color for the border drawn around the 20×20 sequencer region. */
export const COLOR_REGION_BORDER = '#444';

/** Fill color for the shape-placement hover preview (semi-transparent). */
export const COLOR_SHAPE_PREVIEW = 'rgba(100, 180, 255, 0.45)';

/** Stroke color for the scan line that sweeps across the sequencer region. */
export const COLOR_SCAN_LINE    = 'rgba(220, 50, 50, 0.9)';
